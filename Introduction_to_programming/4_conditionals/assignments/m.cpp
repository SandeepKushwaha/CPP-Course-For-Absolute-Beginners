#include<iostream>
using namespace std;

// ==========================================
// Complete the following program.
// ==========================================

int main() {
    int choice;
    cout << "Enter a number (0, 1, 2, 3 or 4) : ";
    cin >> choice;

    switch(choice) {
        case 0:
            // calculate the square of a number here.

        case 1:
            // calculate the cube of a number here.

        case 3:
            // calculate the square root of a number here.

        case 2:
            // check the number is even or odd.

        case 4:
            // check the is divisible by 7 or not.

        default:
            cout << "invalid choice.";
    }

    return 0;
}