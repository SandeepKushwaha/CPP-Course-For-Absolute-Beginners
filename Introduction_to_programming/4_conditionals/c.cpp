#include<iostream>
using namespace std;

// Nested if statement

int main() {
    int x = 10, y = 20;

    if (x > 5) {
        if (y > 15) {
            cout << "x is greater than 5 and y is greater than 15." << endl;
        }
    }

    return 0;
}
