#include<iostream>
using namespace std;

// =======================================================
// Write a program in C++ that takes an integer n as input 
// and prints all the prime numbers between 1 to n.
// =======================================================

int main() {
    int n;
    cout << "Enter a number: ";
    cin >> n;

    cout << "Prime numbers between 1 and " << n << " are: ";

    // complete code here.

    return 0;
}
